---
name: commit
description: Stage, commit with conventional message, and show diff summary
---
Run `git status` and `git diff --stat`, then create a conventional-commit message based on the changes and commit. Do not push.